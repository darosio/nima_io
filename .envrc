# Ensure uv is installed
if ! command -v uv > /dev/null; then
    echo "Error: Install uv first (pip install uv)"
    exit 1
fi

if [ -f uv.lock ]; then
    uv sync --all-extras --frozen
else
    uv sync --all-extras
fi

source .venv/bin/activate
